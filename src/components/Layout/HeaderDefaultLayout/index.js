import Header from '../DefaultLayout/Header';

function HeaderDefaultLayout({ children }) {
    return (
        <div>
            <Header />
            <div className="container">
                <div className="content">{children}</div>
            </div>
        </div>
    );
}

export default HeaderDefaultLayout;
