import { DefaultLayout } from '../.././components/Layout';

function Following() {
    return (
        <DefaultLayout>
            <h1>Following pages</h1>
        </DefaultLayout>
    );
}

export default Following;
